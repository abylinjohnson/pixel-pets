Flask==3.1.3
openai
opencv-python
pygame

# Windows-only: foreground-window, idle time and the transparent overlay window.
pywin32; sys_platform == "win32"

# Windows-only: lets the cat read the active browser tab's URL (UI Automation) for
# smarter distraction detection. Without it, detection falls back to the window title.
uiautomation; sys_platform == "win32"

# On macOS/Linux these stdlib + system tools replace pywin32 (no pip package needed):
#   macOS  : osascript and ioreg ship with the OS — nothing to install.
#   Linux  : install `xdotool` (active window) and `xprintidle` (idle time), e.g.
#            sudo apt install xdotool xprintidle
